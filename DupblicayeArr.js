// const arry=[1,2,1];

// const toFindDuplicates=arry=>arry.filter((item,index)=>arry.indexOfitem(!==)index
// const duplicateElement=toFindDuplicatearry(arry);
// console.log(duplicateElements);


const array=[1, 2, 4, 2, 5,];
const newArr=[];
array.forEach((element, index) => {
    array.indexOf(element)==index && newArr.push(element)
});
console.log(newArr);
        
        
    

